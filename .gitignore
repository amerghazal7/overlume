__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Generated artifacts
gl_montage.png

# CUDA library build/install artifacts
cuda/build/
cuda/install/
cuda/logs/
*.db*CMakeFiles/
.vscode/
graphify-out/

# Claude Code local-only (do not commit machine/session state)
.claude/settings.local.json
.claude/scheduled_tasks.lock
**/build/
# colcon run-from-anywhere artifacts (agents/tests)
log/
install/
cuda/scripts/ros_apps_build/log/
cuda/scripts/ros_apps_build/install/
cuda/src/ros_apps/log/
cuda/src/ros_apps/install/

**/*.mp4

# VM-044: the converted M02P ego glTF is a provisioned local asset (fetch
# script, not plain git — see micropilot_visualization_node/scripts/
# provision_ego_model.sh), not a repo binary. Narrowed to *.glb so
# assets/ego/ATTRIBUTION.md (same convention as assets/fonts, assets/models)
# can still be committed.
cuda/src/ros_apps/src/micropilot_visualization_node/assets/ego/*.glb
