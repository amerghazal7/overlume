# ogm_synthetic.yaml (VM-025). SYNTHETIC, hand-authored -- fixture gap 3:
# no OccupancyGrid topics exist in the recorded bag. 5x2 grid, resolution
# 0.5m, origin (1.0, 2.0, 0.0).
# Row 0 (y=0): -1, 0, 50, 100, 127 -- UnknownCellsBecomeTheSentinelNotTwoFiftyFive
# asserts: -1 and 127 (illegal) both convert to kUnknownCell=255; 0/50/100
# pass through unchanged; exactly one dropped_malformed (127).
# Row 1 (y=1): all zero filler, patched by ogm_update_synthetic.yaml.
header:
  frame_id: map
  stamp: {sec: 0, nanosec: 0}
info:
  resolution: 0.5
  width: 5
  height: 2
  origin:
    position: {x: 1.0, y: 2.0, z: 0.0}
    orientation: {w: 1.0}
data: [-1, 0, 50, 100, 127,
        0, 0,  0,   0,   0]
